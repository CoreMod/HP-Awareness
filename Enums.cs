namespace HPAware
{
    //Unused, but who knows?

    public enum HurtOverlay
    {
        Basic, 
        New,
        Flat
    }

    public enum LowHPOverlay
    {
        Basic,
        New
    }

    public enum LowHPSound
    {
        Bell,
        Heartbeat,
        ManaChirp,
        Click,
        BellNoPitch
    }

    public enum DebuffLayout
    {
        RecentOnly,
        Horizontal,
        Vertical
    }
}
